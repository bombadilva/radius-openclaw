{
  "name": "radius-openclaw",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm -ws run build",
    "test": "npm -ws run test",
    "lint": "npm -ws run lint"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
